<%= form_for @user, url: "/users/login" do |f| %>

    <div class="field"><%= f.label :name, "Choose Name" %><br>
        <%= f.collection_select(:id, @users, :id, :name) %>
    </div>

    <div class="actions">
        <%= f.submit "Sign In" %>
    </div>
<% end %>    